whmcs-state CLI

Usage (placeholder):

```
php cli/bin/whmcs-state validate --env=state/envs/dev
```

The `validate` command currently performs basic checks (file existence and YAML parse if `yaml` extension installed).
